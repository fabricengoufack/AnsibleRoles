# Playbook to call multiple roles to configure systems
- name: Configure systems
  hosts: sac
  become: yes

  tasks:

- name: Call Roles
  hosts: sac
  roles:
   #- hostname
   #- firewalld
   #- ntp-config
   - motd
